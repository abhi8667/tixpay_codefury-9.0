<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">true</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#0A0C10</item>
    <!-- The status bar is hidden app-wide, but via WindowInsetsController in
         MainActivity rather than android:windowFullscreen. The legacy flag
         hides the bar and also breaks adjustResize, which would leave the
         keyboard sitting on top of the chat bar and the onboarding inputs. -->
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
  </style>
</resources>
